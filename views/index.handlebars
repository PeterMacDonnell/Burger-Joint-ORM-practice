<h1>Welcome to THICC Burger</h1>

<h2>These burgers are still available to eat.</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> layouts/main.handlebars devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>These burgers...Have been DEVOURED.</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> layouts/main.handlebars devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Make your own Burger!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> devoured!<br>
    <input type="radio" name="devoured" value="0"> Awake
  </div>

  <button type="submit">Add burger</button>
</form>
